# Secrets — real keys live here, NEVER in git
bridge/.env
.env
worker/.dev.vars

# NOTE: config.js IS committed on purpose — it holds only PUBLIC-SAFE values
# (Firebase web config + empty secret slots). The Gemini key never goes there.

# Deps + OS junk
node_modules/
.bughunt/
.DS_Store

# Vosk wake-word model (~53MB) — served locally by the bridge, never in git
models/

# Home lamp↔room map — personal home topology (entity ids, room names). Lives on
# the Mac's bridge, auto-generated/learned; keep it OUT of the public repo.
bridge/ha-rooms.json
